# How to update the iotd-imx8p-config package

## Follow the procedure:

* Get admin credentials:
```
sudo -i
```

* Run this command:

```
bash <(curl -L https://raw.githubusercontent.com/compulab-yokneam/bin/refs/heads/balena/iotd-imx8p/run.me)
```
