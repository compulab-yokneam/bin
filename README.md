# How to deplot the device tree file to the boot folder

## Follow the procedure:

* Get admin credentials:
```
sudo -i
```

* Run this command:
```
bash <(curl -L https://raw.githubusercontent.com/compulab-yokneam/bin/refs/heads/dtbs/run.me)
```
